{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug worldgen (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/worldgen",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}